services:

  jellyfin:
    container_name: jellyfin
    image: jellyfin/jellyfin:latest
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-file: ${DOCKERLOGGING_MAXFILE}
        max-size: ${DOCKERLOGGING_MAXSIZE}
    user: 1000:10
    ports:
      - 8096:8096/tcp
      - 7359:7359/udp
    environment:
      - TZ=${TZ} 
    volumes:
      - ./jellyfin/config:/config:rw
      - ./jellyfin/cache:/cache:rw
      - ./jellyfin/logs:/logs:rw
      - ${ARES_DATADIR}/media:/media:rw
    devices:
      - /dev/dri/renderD128:/dev/dri/renderD128
      - /dev/dri/card0:/dev/dri/card0
      - /dev/dri:/dev/dri

  radarr:
    container_name: radarr
    image: ghcr.io/hotio/radarr:release
    privileged: true
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-file: ${DOCKERLOGGING_MAXFILE}
        max-size: ${DOCKERLOGGING_MAXSIZE}
    ports:
      - 7878:7878
    environment:
      - PUID=1000
      - PGID=10
      - TZ=${TZ}
      - UMASK=002
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ./radarr:/config
      - ${ARES_DATADIR}:/data

  sonarr:
    container_name: sonarr
    image: ghcr.io/hotio/sonarr:release
    privileged: true
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-file: ${DOCKERLOGGING_MAXFILE}
        max-size: ${DOCKERLOGGING_MAXSIZE}
    ports:
      - 8989:8989
    environment:
      - PUID=1000
      - PGID=10
      - TZ=${TZ}
      - UMASK=002
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ./sonarr:/config
      - ${ARES_DATADIR}:/data

  bazarr:
    container_name: bazarr
    image: ghcr.io/hotio/bazarr:release
    privileged: true
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-file: ${DOCKERLOGGING_MAXFILE}
        max-size: ${DOCKERLOGGING_MAXSIZE}
    ports:
      - 6767:6767
    environment:
      - PUID=1000
      - PGID=10
      - TZ=${TZ}
      - UMASK=002
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ./bazarr:/config
      - ${ARES_DATADIR}/media:/data/media

  prowlarr:
    container_name: prowlarr
    image: ghcr.io/hotio/prowlarr:release
    privileged: true
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-file: ${DOCKERLOGGING_MAXFILE}
        max-size: ${DOCKERLOGGING_MAXSIZE}
    ports:
      - 9696:9696
    environment:
      - PUID=1000
      - PGID=10
      - TZ=${TZ}
      - UMASK=002
      - ARGS=
    volumes:
      - ./prowlarr:/config:rw

  transmission:
    container_name: transmission
    image: lscr.io/linuxserver/transmission
    privileged: true
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-file: ${DOCKERLOGGING_MAXFILE}
        max-size: ${DOCKERLOGGING_MAXSIZE}
    ports:
      - 9091:9091
      - 51413:51413
    environment:
      - PUID=1000
      - PGID=10
      - TZ=${TZ}
      - VERSION=docker
    volumes:
      - ./transmission/config:/config
      - ${ARES_DATADIR}/torrents:/data/torrents
